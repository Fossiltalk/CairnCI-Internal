{
  "$comment": "Metadata the CLI can see but the Metadata API cannot return, or returns incompletely. Tracked in the branch so a run can tell a user 'this is expected' instead of 'something broke'. Every entry carries a source; entries marked verified:'observed' were confirmed against a real org from this repo, verified:'docs' are from Salesforce documentation. This is a SEED, not an exhaustive list — extend it from real run output rather than guessing.",
  "$comment2": "Salesforce also documents whole FEATURES whose metadata types are unavailable API-wide. That list changes every release and is not duplicated here; see canonicalDocs below.",
  "canonicalDocs": {
    "unsupportedMetadataTypes": "https://developer.salesforce.com/docs/atlas.en-us.api_meta.meta/api_meta/meta_unsupported_types.htm",
    "metadataTypesList": "https://developer.salesforce.com/docs/atlas.en-us.api_meta.meta/api_meta/meta_types_list.htm",
    "listMetadata": "https://developer.salesforce.com/docs/atlas.en-us.api_meta.meta/api_meta/meta_listmetadata.htm"
  },
  "categories": {
    "api-limitation": "The Metadata API itself cannot return this. Nothing to fix — expected.",
    "permission": "The running user most likely lacks access. Grant the permission and re-run.",
    "feature-not-enabled": "The related feature is not enabled in this org.",
    "cli-registry-gap": "The org has the type but the installed @salesforce/cli does not know it. Upgrade the CLI.",
    "managed-package": "Managed-package internals are not independently retrievable."
  },
  "types": {
    "StandardValueSet": {
      "category": "api-limitation",
      "reason": "listMetadata returns zero rows for StandardValueSet, so this tool's index cannot discover its members. The type itself IS retrievable — but only when each value set is named explicitly in package.xml.",
      "workaround": "Add the standard value sets you care about to a hand-maintained manifest and retrieve them separately, or use Workbench > Migration > Retrieve with an explicit package.xml. There is no way to enumerate them from the API.",
      "source": "https://github.com/mdapi-issues/listmetadata-standardvalueset",
      "verified": "docs"
    },
    "Report": {
      "category": "api-limitation",
      "reason": "Reports in a user's personal folder ('My Personal Custom Reports') are not exposed to the Metadata API — it covers global metadata components only. Reports in shared folders and in unfiled$public retrieve normally.",
      "workaround": "None for personal folders. Move a report into a shared folder if it needs to be source-tracked. Expect the retrieved count to be lower than what the Reports tab shows.",
      "source": "https://ideas.salesforce.com/s/idea/a0B8W00000GdhhrUAB/metadata-api-access-to-unfiled-public-reports-and-my-personal-reports",
      "verified": "docs",
      "appliesTo": "personal-folder members only"
    },
    "Dashboard": {
      "category": "api-limitation",
      "reason": "Same personal-folder restriction as Report: dashboards in a user's private folder are not global metadata and are not returned.",
      "workaround": "Move the dashboard to a shared folder to make it retrievable.",
      "source": "https://ideas.salesforce.com/s/idea/a0B8W00000GdhhrUAB/metadata-api-access-to-unfiled-public-reports-and-my-personal-reports",
      "verified": "docs",
      "appliesTo": "personal-folder members only"
    },
    "ConnectedApp": {
      "category": "api-limitation",
      "reason": "Retrieves, but the OAuth consumer secret is redacted from Metadata API responses. The retrieved XML is therefore not sufficient to recreate a working connected app in another org.",
      "workaround": "Read the consumer key/secret from Setup > App Manager > Manage Consumer Details, and set the secret by hand in the destination org after deploying.",
      "source": "https://help.salesforce.com/s/articleView?id=000390339&language=en_US&type=1",
      "verified": "docs",
      "appliesTo": "consumer secret only; the rest of the component retrieves normally"
    },
    "CustomMetadata": {
      "category": "api-limitation",
      "reason": "listMetadata silently caps at 3,000 rows with no pagination, and CustomMetadata records span arbitrary __mdt objects with no single queryable Tooling object, so there is no fallback query to recover the full list. In an org with more than 3,000 custom metadata records the index under-counts this type.",
      "workaround": "Discover each custom metadata type and query its records individually, then retrieve with an explicit manifest. If you need this routinely, add a fallback for it in lib/tooling-fallback.mjs.",
      "source": "https://developer.salesforce.com/docs/atlas.en-us.api_meta.meta/api_meta/meta_listmetadata.htm",
      "verified": "observed"
    },
    "IdentityVerificationProcDtl": {
      "category": "cli-registry-gap",
      "reason": "A Public Sector Solutions type that exists in the org and appears in describeMetadata/listMetadata output, but is missing from the installed @salesforce/cli's bundled metadata registry. The CLI rejects the whole manifest before contacting the org, so one unknown type fails an entire chunk.",
      "workaround": "The run auto-recovers: the offending type is dropped from the chunk and the retrieve is retried (see lib/retrieve-metadata.mjs). To actually capture the type, upgrade @salesforce/cli and re-run. Any Industries/PSS type can hit this after a Salesforce release the CLI has not caught up with.",
      "source": "https://github.com/forcedotcom/cli/issues",
      "verified": "observed"
    }
  }
}
